diff --git a/node_modules/zca-js/dist/apis/listen.js b/node_modules/zca-js/dist/apis/listen.js
index ce5d95e..442e1b4 100644
--- a/node_modules/zca-js/dist/apis/listen.js
+++ b/node_modules/zca-js/dist/apis/listen.js
@@ -154,6 +154,10 @@ export class Listener extends EventEmitter {
                 if (decodedData.length == 0)
                     return;
                 const parsed = JSON.parse(decodedData);
+                console.log(`[ZCA-JS] WS Received cmd=${cmd} subCmd=${subCmd} version=${version}`);
+                if (version == 1 && ![1, 2, 501, 521, 601, 701, 1500, 2600].includes(cmd)) {
+                    console.log(`[ZCA-JS] Unhandled cmd=${cmd} subCmd=${subCmd}:`, parsed);
+                }
                 if (version == 1 && cmd == 1 && subCmd == 1 && hasOwn(parsed, "key")) {
                     this.cipherKey = parsed.key;
                     this.emit("cipher_key", parsed.key);
@@ -172,7 +176,7 @@ export class Listener extends EventEmitter {
                         ping();
                     }, this.ctx.settings.features.socket.ping_interval);
                 }
-                if (version == 1 && cmd == 501 && subCmd == 0) {
+                if (version == 1 && cmd == 501) {
                     const parsedData = (await decodeEventData(parsed, this.cipherKey)).data;
                     const { msgs } = parsedData;
                     for (const msg of msgs) {
@@ -191,7 +195,7 @@ export class Listener extends EventEmitter {
                         }
                     }
                 }
-                if (version == 1 && cmd == 521 && subCmd == 0) {
+                if (version == 1 && cmd == 521) {
                     const parsedData = (await decodeEventData(parsed, this.cipherKey)).data;
                     const { groupMsgs } = parsedData;
                     for (const msg of groupMsgs) {
diff --git a/node_modules/zca-js/dist/apis/sendVideo.d.ts b/node_modules/zca-js/dist/apis/sendVideo.d.ts
index 23306ee..4b1bce3 100644
--- a/node_modules/zca-js/dist/apis/sendVideo.d.ts
+++ b/node_modules/zca-js/dist/apis/sendVideo.d.ts
@@ -16,6 +16,7 @@ export type SendVideoOptions = {
      * Video duration in milliseconds || Eg: video duration: 5.5s => 5.5 * 1000 = 5500
      */
     duration?: number;
+    fileSize?: number;
     /**
      * Width of the video
      */
@@ -28,6 +29,8 @@ export type SendVideoOptions = {
      * Time to live in milliseconds (default: 0)
      */
     ttl?: number;
+    fileId?: string;
+    checksum?: string;
 };
 export type SendVideoResponse = {
     msgId: number;
diff --git a/node_modules/zca-js/dist/apis/sendVideo.js b/node_modules/zca-js/dist/apis/sendVideo.js
index 03c818c..e134662 100644
--- a/node_modules/zca-js/dist/apis/sendVideo.js
+++ b/node_modules/zca-js/dist/apis/sendVideo.js
@@ -31,9 +31,13 @@ export const sendVideoFactory = apiFactory()((api, ctx, utils) => {
         let fileSize = 0;
         const clientId = Date.now();
         try {
-            const headResponse = await utils.request(options.videoUrl, { method: "HEAD" }, true);
-            if (headResponse.ok) {
-                fileSize = parseInt(headResponse.headers.get("content-length") || "0");
+            if (options.fileSize) {
+                fileSize = options.fileSize;
+            } else {
+                const headResponse = await utils.request(options.videoUrl, { method: "HEAD" }, true);
+                if (headResponse.ok) {
+                    fileSize = parseInt(headResponse.headers.get("content-length") || "0");
+                }
             }
         }
         catch (error) {
@@ -52,11 +56,14 @@ export const sendVideoFactory = apiFactory()((api, ctx, utils) => {
                     duration: (_b = options.duration) !== null && _b !== void 0 ? _b : 0,
                     width: (_c = options.width) !== null && _c !== void 0 ? _c : 1280,
                     height: (_d = options.height) !== null && _d !== void 0 ? _d : 720,
+                    ratio: (options.width || 1280) / (options.height || 720),
                     fileSize: fileSize,
+                    fileId: options.fileId,
+                    checksum: options.checksum,
                     properties: {
                         color: -1,
                         size: -1,
-                        type: 1003,
+                        type: 1,
                         subType: 0,
                         ext: {
                             sSrcType: -1,
@@ -81,11 +88,14 @@ export const sendVideoFactory = apiFactory()((api, ctx, utils) => {
                     duration: (_g = options.duration) !== null && _g !== void 0 ? _g : 0,
                     width: (_h = options.width) !== null && _h !== void 0 ? _h : 1280,
                     height: (_j = options.height) !== null && _j !== void 0 ? _j : 720,
+                    ratio: (options.width || 1280) / (options.height || 720),
                     fileSize: fileSize,
+                    fileId: options.fileId,
+                    checksum: options.checksum,
                     properties: {
                         color: -1,
                         size: -1,
-                        type: 1003,
+                        type: 1,
                         subType: 0,
                         ext: {
                             sSrcType: -1,
diff --git a/node_modules/zca-js/dist/apis/sendVoice.d.ts b/node_modules/zca-js/dist/apis/sendVoice.d.ts
index c04c728..7355776 100644
--- a/node_modules/zca-js/dist/apis/sendVoice.d.ts
+++ b/node_modules/zca-js/dist/apis/sendVoice.d.ts
@@ -1,6 +1,10 @@
 import { ThreadType } from "../models/index.js";
 export type SendVoiceOptions = {
     voiceUrl: string;
+    duration?: number;
+    fileId?: string;
+    checksum?: string;
+    fileSize?: number;
     /**
      * Time to live in milliseconds (default: 0)
      */
diff --git a/node_modules/zca-js/dist/apis/sendVoice.js b/node_modules/zca-js/dist/apis/sendVoice.js
index f8bcc6b..995af05 100644
--- a/node_modules/zca-js/dist/apis/sendVoice.js
+++ b/node_modules/zca-js/dist/apis/sendVoice.js
@@ -19,15 +19,18 @@ export const sendVoiceFactory = apiFactory()((api, ctx, utils) => {
         var _a, _b;
         let fileSize = null;
         const clientId = Date.now().toString();
-        try {
-            const headResponse = await utils.request(options.voiceUrl, { method: "HEAD" }, true);
-            if (headResponse.ok) {
-                fileSize = parseInt(headResponse.headers.get("content-length") || "0");
+        if (options.fileSize) {
+            fileSize = options.fileSize;
+        } else {
+            try {
+                const headResponse = await utils.request(options.voiceUrl, { method: "HEAD" }, true);
+                if (headResponse.ok) {
+                    fileSize = parseInt(headResponse.headers.get("content-length") || "0");
+                }
+            } catch (error) {
+                // Ignore
             }
         }
-        catch (error) {
-            throw new ZaloApiError(`Unable to get voice content: ${error instanceof Error ? error.message : String(error)}`);
-        }
         const params = type === ThreadType.User
             ? {
                 toId: threadId,
@@ -37,8 +40,12 @@ export const sendVoiceFactory = apiFactory()((api, ctx, utils) => {
                 clientId: clientId,
                 msgInfo: JSON.stringify({
                     voiceUrl: options.voiceUrl,
-                    m4aUrl: options.voiceUrl,
+                    m4a: options.voiceUrl, m4aUrl: options.voiceUrl,
                     fileSize: fileSize !== null && fileSize !== void 0 ? fileSize : 0,
+                    duration: options.duration || 0,
+                    fileId: options.fileId || "",
+                    checksum: options.checksum || "",
+                    waveformSamples: [0,234,445,615,1083,844,378,319,309,299,303,284,261,269,272,249,244,256,245,270,271,313,371],
                 }),
                 imei: ctx.imei,
             }
@@ -51,8 +58,13 @@ export const sendVoiceFactory = apiFactory()((api, ctx, utils) => {
                 clientId: clientId,
                 msgInfo: JSON.stringify({
                     voiceUrl: options.voiceUrl,
+                    m4a: options.voiceUrl, m4aUrl: options.voiceUrl,
                     m4aUrl: options.voiceUrl,
                     fileSize: fileSize !== null && fileSize !== void 0 ? fileSize : 0,
+                    duration: options.duration || 0,
+                    fileId: options.fileId || "",
+                    checksum: options.checksum || "",
+                    waveformSamples: [0,234,445,615,1083,844,378,319,309,299,303,284,261,269,272,249,244,256,245,270,271,313,371],
                 }),
                 imei: ctx.imei,
             };
diff --git a/node_modules/zca-js/dist/cjs/apis/listen.cjs b/node_modules/zca-js/dist/cjs/apis/listen.cjs
index 92ab353..0ba3576 100644
--- a/node_modules/zca-js/dist/cjs/apis/listen.cjs
+++ b/node_modules/zca-js/dist/cjs/apis/listen.cjs
@@ -110,7 +110,7 @@ class Listener extends EventEmitter {
     start({ retryOnClose = false } = {}) {
         if (this.ws)
             throw new ZaloApiError.ZaloApiError("Already started");
-        const ws = new WebSocket(this.wsURL, {
+        const wsOptions = {
             headers: {
                 "accept-encoding": "gzip, deflate, br, zstd",
                 "accept-language": "en-US,en;q=0.9",
@@ -125,8 +125,11 @@ class Listener extends EventEmitter {
                 "user-agent": this.userAgent,
                 cookie: this.cookie,
             },
-            agent: this.ctx.options.agent
-        });
+        };
+        if (this.ctx.options.agent) {
+            wsOptions.agent = this.ctx.options.agent;
+        }
+        const ws = new WebSocket(this.wsURL, wsOptions);
         this.ws = ws;
         ws.onopen = () => {
             this.onConnectedCallback();
@@ -184,7 +187,7 @@ class Listener extends EventEmitter {
                         ping();
                     }, this.ctx.settings.features.socket.ping_interval);
                 }
-                if (version == 1 && cmd == 501 && subCmd == 0) {
+                if (version == 1 && cmd == 501) {
                     const parsedData = (await utils.decodeEventData(parsed, this.cipherKey)).data;
                     const { msgs } = parsedData;
                     for (const msg of msgs) {
@@ -203,7 +206,7 @@ class Listener extends EventEmitter {
                         }
                     }
                 }
-                if (version == 1 && cmd == 521 && subCmd == 0) {
+                if (version == 1 && cmd == 521) {
                     const parsedData = (await utils.decodeEventData(parsed, this.cipherKey)).data;
                     const { groupMsgs } = parsedData;
                     for (const msg of groupMsgs) {
diff --git a/node_modules/zca-js/dist/cjs/apis/sendVideo.cjs b/node_modules/zca-js/dist/cjs/apis/sendVideo.cjs
index df95386..ce6d86b 100644
--- a/node_modules/zca-js/dist/cjs/apis/sendVideo.cjs
+++ b/node_modules/zca-js/dist/cjs/apis/sendVideo.cjs
@@ -63,7 +63,12 @@ const sendVideoFactory = utils.apiFactory()((api, ctx, utils) => {
                     duration: (_b = options.duration) !== null && _b !== void 0 ? _b : 0,
                     width: (_c = options.width) !== null && _c !== void 0 ? _c : 1280,
                     height: (_d = options.height) !== null && _d !== void 0 ? _d : 720,
+                    ratio: (options.width || 1280) / (options.height || 720),
+                    thumbWidth: options.width,
+                    thumbHeight: options.height,
                     fileSize: fileSize,
+                    fileId: options.fileId,
+                    checksum: options.checksum,
                     properties: {
                         color: -1,
                         size: -1,
@@ -92,7 +97,12 @@ const sendVideoFactory = utils.apiFactory()((api, ctx, utils) => {
                     duration: (_g = options.duration) !== null && _g !== void 0 ? _g : 0,
                     width: (_h = options.width) !== null && _h !== void 0 ? _h : 1280,
                     height: (_j = options.height) !== null && _j !== void 0 ? _j : 720,
+                    ratio: (options.width || 1280) / (options.height || 720),
+                    thumbWidth: options.width,
+                    thumbHeight: options.height,
                     fileSize: fileSize,
+                    fileId: options.fileId,
+                    checksum: options.checksum,
                     properties: {
                         color: -1,
                         size: -1,
diff --git a/node_modules/zca-js/dist/cjs/context.cjs b/node_modules/zca-js/dist/cjs/context.cjs
index aa2c571..8ce7a92 100644
--- a/node_modules/zca-js/dist/cjs/context.cjs
+++ b/node_modules/zca-js/dist/cjs/context.cjs
@@ -21,6 +21,7 @@ const createContext = (apiType = 30, apiVersion = 671) => ({
         checkUpdate: true,
         logging: true,
         polyfill: global.fetch,
+        agent: null,
     },
     secretKey: null,
 });
